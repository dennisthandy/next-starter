import styles from '../styles/components/footer.module.scss';

const Footer: React.FC = () => {
    return (
        <footer className={styles.footer}>
            <span>@denisthandy NextJS Boilerplate 2021</span>
        </footer>
    );
};

export default Footer;
